apiVersion: nc.waveywaves.com/v1beta1
kind: Nextcloud
metadata:
  labels:
    app.kubernetes.io/name: nextcloud-operator
    app.kubernetes.io/managed-by: kustomize
  name: nextcloud-sample
spec:
  # TODO(user): Add fields here
